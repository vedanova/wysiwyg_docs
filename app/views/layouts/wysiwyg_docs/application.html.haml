!!!
%html
  %head
    %meta
      = stylesheet_link_tag    "wysiwyg_docs/application", :media => "all"
      = javascript_include_tag "wysiwyg_docs/application"
      = csrf_meta_tags
    - if content_for?(:title)
      %title
        =yield :title
        |
        = WysiwygDocs.page_title
    - else
      %title= WysiwygDocs.page_title
  %body.wysiwyg_docs
    .navbar.navbar-fixed-top
      .navbar-inner
        = link_to WysiwygDocs.brand_name, main_app.root_path, class: "brand"
        %ul.nav
          %li= link_to WysiwygDocs.index_title, docs_path
          - if current_user && current_user.admin?
            %li
              = link_to "Topics", topics_path

    #main{:role => "main"}
      .container-fluid
        .row-fluid.row
          .span12
            = render 'layouts/messages'
      .container-fluid
        %section.row-fluid.row
          = yield

