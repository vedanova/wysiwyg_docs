%aside.span3
  .well.sidebar-nav-fixed
    %ul.nav.nav-list
      - WysiwygDocs::Topic.all.each do |topic|
        %li.nav-header.topic
          = topic.name
          - topic.articles.each do |article|
            %li= link_to(article.title, topic_article_path(topic, article))
